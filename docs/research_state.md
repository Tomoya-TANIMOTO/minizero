# research_state — 研究の現在地

最終更新：2026-07-20（旧版を差し替えること．旧版は Day1/Day2 未着手前提で古い）

このファイルはプロジェクト内の各チャットが共通で参照する「現在地」．
メモリ更新には遅延があるため，**最新の確定事項はこのファイルを正とする**．

---

## 1. 完了済み [事実]

### 1.1 GUI 符号バグ（トラック完了）

`xrl_viz/index.html` の符号フレームバグは**3箇所すべて修正済み**．

| 箇所 | 状態 |
|------|------|
| 探索木ペインの Q 表示 | 修正済み |
| 評価軌跡グラフ（drawChart） | 修正済み |
| eval パネル（現盤面）の Q・v 表示 | 修正済み |

修正規則：子ノードは `n.player` で反転，root は `to_play` で反転（root は `player:null`）．
`tree_json` の Q は `node->getMean()`（raw fixed-frame）で，決定に使う `getNormalizedMean()`
（手番視点・virtual loss 込み）とはフレームが異なる．

### 1.2 n スイープ（取得・分析済み）

決定論 eval config で n = 16 / 24 / 32 / 64 / 128 / 256 の記録を取得済み．

- **halving 発火閾値は n=32（実測確認）**．n=16/24 では発火しない．
- 方策順位の逆転：n=16 は 0/55 ply，n=24 は 0/58 ply，**n≥32 は 42–43/59 ply**．
- **n を増やしても強さは向上しない**（評価差 ≤ 0.17）．
  → 研究上の n の価値は「強さ」ではなく「説明可能性」にある．

### 1.3 Gumbel パラメータ（ソース確認済み）

- `actor_gumbel_sample_size`（= m）：既定値 **16 の固定値**．論文 Go 実験の m=min(n,16) ではない．
  [configuration.cpp L31]
- 初期 budget：`max(1, floor( n / (log2(m) * sample_size_) ))` [gumbel_zero.cpp L99]
- **発火判定に使うのは next_budget**：`floor( n / (log2(m) * sample_size_ / 2) )`，
  条件は `next_budget > 0 && sample_size_ > 2` [gumbel_zero.cpp L109-110]
  → 過去メモの `floor(16/32)` は **正しい**（分母は sample_size_/2 側）．
- halving は `sample_size_ > 2` で停止する [L110]．

### 1.4 tree_json の拡張フィールド

`gaz_eliminated_round`（halving の何巡目で候補から落ちたか）と
`gaz_decision_score`（決定時スコア）が出力されるようになっている．
**これらは説明パイプラインの述語として直接使える**（後述）．

---

## 2. 未解決・進行中

### 2.1 sequentialHalving 内の virtual loss バグ（upstream 責任）[事実]

`afterNNEvaluation` L102 が `removeVirtualLoss` L160 より先に走るため，
halving の候補足切り（`sortCandidatesByScore` → `getNormalizedMean`，vl を織り込む）が
virtual loss 汚染された値で行われる．

**方針（教授指示）**：これは台湾チーム（rlglab/minizero）が直すべきもの．
GitHub issue を出して向こうに対応してもらう．こちらでは証拠を集める．

**ただし研究データ上の扱いは別** [推論]：
halving が発火するのは n≥32 なので，n=16 ではこのバグは眠っている．
説明性のために n を上げる今回の方針は，このバグを起こしにいく方向．
→ **自分の fork にはローカル修正を入れた状態でコーパスを生成する**（前回 `c6284dc` と同じ立て付け）．

#### 2.1.1 対応の結果（2026-07-21）[事実]

計装・実測・ローカル修正・issue 草案まで完了．

- 計装：`gaz_eliminated_round` / `gaz_decision_score` を tree_json に出力（`4439cd4`）．
  §1.4 は記述時点では**事実に反していた**（フィールドは存在しなかった）．いまは存在する．
- ローカル修正：候補足切りの採点から virtual loss を外した（`c8117dc`）．
  淘汰のタイミングは変えていない．
- issue 草案：`docs/upstream_issue_gumbel_virtual_loss.md`（英語・**未提出**）．

同一局面 48 ply（`replay.py` で n16 の局面列を各エンジンに打ち直し）での修正前後の差：

| n | 着手が変わった ply | 訪問数分布が変わった ply | 淘汰が変わった巡 |
|---|---|---|---|
| 16 | 0 | 0/48 | 0/0（halving が発火しない） |
| 32 | 1 | 6/48 | 9/144 |
| 128 | 3 | 9/48 | 13/144 |

- §2.1 の [推論]「n=16 では差が出ず n≥32 で差が出る」は**実測で支持された**ので [事実] に上げてよい．
- 各巡で落ちる候補の**件数**は修正前後で完全に一致（巡0:147 / 巡1:157 / 巡2:95）．
  変わるのは「どの候補が落ちるか」だけで，halving の日程は変わっていない．
- 機構レベルの結論（発火は n≥32，初期予算 `max(1, floor(n/64))`）は**変わっていない**．
- 自己対戦学習への影響：**なし**．virtual loss を触るのは `step()` だけ
  （zero_actor.cpp L150, L160）で，自己対戦は actor_group.cpp L91 から
  `afterNNEvaluation` を直接呼ぶため `virtual_loss_` は常に 0．前回 `c6284dc` と同じ論拠が成立する．
- 凍結局面：**6局面＋対照のすべてが修正前後で着手・訪問数・Q・v とも完全に一致**し，性質を維持．
  当初「ply 58 が未検証」と報告したが，理由の説明が誤っていた．正しくは
  **局面4・5・6 は n16 ではなく n=128 の対局から取られており**，n16 を流す `replay.py` の
  対象外だっただけである．`xrl_viz/replay_line.py` で n128 の着手列を打ち直して検証済み
  （`xrl_viz/data/ply58_check/`）．
- 上記に伴い `frozen_positions.md` の同定方法を改訂した．修正前後で n=128 の自己対戦は
  **ply 13 の着手から分岐**するため，「対局の何 ply 目」という参照はエンジンが変わると壊れる．
  6局面＋対照とも**着手列＋盤面図**での同定に統一した（着手列で 7/7 の盤面再現を確認済み）．
- `cf20e4c`「tree_json のフィルタ修正」で，root 直下は訪問0の手も出すようにした．
  これで top-m に入らなかった手（`gaz_eliminated_round = -2`）が記録に残る．
  §2.1c 末尾の「-2 は tree_json に出ない」という記述はこの修正で解消済み．

##### 既存記録との食い違い [要確認]

2026-07-14 の記録「淘汰結果が変わったラウンド数：n=32 で 8/144，n=128 で 9/144」と，
今回の実測（n=32 で 9/144，n=128 で 13/144）が**一致しない**．どちらが正しいかは判断していない．

観測できた事実だけ記す：今回の測定で「eliminated_round が変わった **ply** 数」は
n=32 で 8/48，n=128 で 9/48 であり，記録された 8 と 9 に一致する．
分母 144（= 48 ply × 3 巡）は両者で共通．
記録側が巡ではなく ply を数えていた可能性があるが，一次データが残っていないため確認できない．

### 2.1b コンソール出力の混線（VL バグとは別件）[事実]

`tools/quick-run.sh console` 経由だと，色付けされた盤面表示（`quick-run.sh:634` の
非同期プロセス置換）が stdout の GTP 応答行の途中に割り込み，tree_json が壊れる．
実測で 64 件中 48 件しかパースできなかった．バイナリ直接起動なら 65/65 件通る．

さらに `Capture_game.sh` が壊れた行を黙って捨てていたため，`trees[]` が縮んで
board と root の対応が 1 ply ずれた記録ができていた．

- fork 側は `f60cb87` で修正済み（直接起動＋壊れていたらエラー終了）．
- **既存コーパスへの被害はゼロ**．`xrl_viz/check_alignment.py` で全件検査し，
  sweep 全 n 水準（各 64 ply）・baseline（64 ply）・replay_n{16,32,128}（各 48 ply）
  すべて不整合 0 件．`replay.py` は元からバイナリを直接起動しており影響を受けない．
  → §1.2 の n スイープの数値と `frozen_positions.md` の局面選定は，
  **この件を理由に無効化する必要はない**．
- upstream issue の2件目として草案あり：`docs/upstream_issue_console_capture.md`．
  **VL の issue が片付いてから出す．**

### 2.1c 合法手が m を超える局面（集計のみ，扱いは未決）[事実]

`actor_gumbel_sample_size`（= m = 16）を合法手数 k が超える ply の数：

| n | 16 | 24 | 32 | 64 | 128 | 256 |
|---|---|---|---|---|---|---|
| k>16 の ply | 0/64 | 3/64 | 0/64 | 1/64 | 3/64 | 4/64 |
| 最大 k | 15 | 18 | 14 | 17 | 17 | 19 |

n=128 の該当 3 ply（24 / 26 / 30）では合法手 G2 が探索木に一切現れない（N=0，応手ゼロ）．
それ以外の ply では全子が N≥2．

説明設計への影響はここでは判断しない（教授と詰める）．

なお，top-m に入らなかった手の印（`gaz_eliminated_round = -2`）は当初 tree_json に
出なかった（`displayInTreeLog()` が `count_ > 0` の子だけを出すため）．
`cf20e4c`「tree_json のフィルタ修正」で root 直下だけフィルタを外し，解消済み．

### 2.2 「複数局面」の解釈（教授指示の訂正）[事実：教授発言]

教授の言う「様々な盤面・複数局面」は **1局内の複数局面ではなく，複数の対局**の意味．
→ 序盤手を固定して多様な決定論対局を生成する必要がある．

#### 2.2.1 コーパス生成の結果（2026-07-21）[事実]

**3手固定・4系統・n=16/32/128** で生成済み．一覧は `xrl_viz/data/corpus/README.md`．

| 記号 | 着手列 | 位置づけ | 勝敗（主軸） |
|---|---|---|---|
| S1 | E3 D3 C4 | 系統1（対角） | 白勝ち 33-31 |
| S2 | E3 F3 F4 | 系統2 | 黒勝ち 41-23 |
| S3 | E3 F5 D6 | 系統3 | 黒勝ち 34-30 |
| S4 | E3 D3 C6 | 系統1 の別分岐 | 引分 32-32 |

- 主軸（方式2・打ち直し）12記録 `replay_S{1..4}_n{16,32,128}.json`．
  各系統を n=128 で対局させた着手列を3水準に打ち直したもので，全 ply を直接比較できる．
- 補助（方式1・独立対局）3記録 `indep_S1_n{16,32,128}.json`．
- 源泉 4記録 `src_S{1..4}_n128.json`．
- 生成エンジン：`fd5d477`（`c8117dc` と `cf20e4c` を含む）．
- 健全性：全19記録で不整合 0 件．p_noise は全ノードで 0 で決定論条件が成立．
- 序盤系統の根拠は `tools/opening_branches.py`（3手固定で 14 系統）．

**n による着手の分岐** [事実]：

| 系統 | 最初に分岐した ply | 分岐した ply 数 |
|---|---|---|
| S1 | 4（n=16 は F4，n=32/128 は C5） | 29 |
| S2 | 4（n=16 は C4，n=32/128 は E2） | 28 |
| S3 | 9（n=16 は C5，n=32/128 は G3） | 31 |
| S4 | 11（n=16 は E2，n=32/128 は B5） | 22 |

4系統とも **n=16 だけが先に外れ，n=32 と n=128 は途中まで一致する**．
補助3記録（独立対局）では ply 4 で分岐し，61 ply 中 53 ply で着手が食い違った．

解釈は Task 3 で扱う．ここでは事実のみ記す．

### 2.3 上位2手の訪問数タイ [事実]（2026-07-21 実測で確定）

当初の仮説「n をいくら増やしても上位2手の訪問数が同数になる」は，
そのままの形では成立しない．実測は n 依存だった．

| n | 生存2手の ply | N 一致 | 一致率 |
|---|---|---|---|
| 16 | 1/64 | — | 検証対象外（halving が1巡も走らない） |
| 32 | 58/64 | 38 | 65.5% |
| 128 | 56/64 | 56 | 100% |

n=32 の不一致 20 件は**すべて差が 1**（7 対 6，8 対 7 等）．
[推論] n=128 では予算式の各段が割り切れる（2→6→14→30）が，
n=32 では floor で端数が落ちるため，最後の配分で 1 回ぶんの余りが出ると考えられる．
[要確認] 未検証．

**説明設計にとっての帰結は変わらない** [推論]：
問うべきは「訪問数の差が手の優劣を語る根拠になるか」である．
n=128 では差ゼロ，n=32 でも最大 1（7 対 6）であり，
「エージェントはこの手を最も多く読んだ」という説明はどの n でも成立しない．
根拠が「構造上必ず同数」から「実測すると差は 0 か 1」に変わっただけで，
説明の語り口を訪問数以外に置く必要がある点は変わらない．

一次データ：`xrl_viz/data/analysis_taskB/survivors_n{16,32,128}.csv`

---

## 3. 教授の指示

### 3.1 従来の指示（優先順位付き）[事実：教授発言]

① n を増やす（= `actor_num_simulation`）．理由は「n=16 or 17 だと探索木が小さすぎて説明しにくい」
② 様々な盤面・**複数の対局**を用意する
③ 着手理由を素人でもわかるように説明し，その証拠を集める＝それが複数対局
   明示的除外：説明に Gumbel スコア等の内部機構を出さない
④ trace-base：探索木そのものを根拠に説明する

### 3.2 追加された指示（新）[事実：教授発言]

- 俺らがやりたいのは**探索木をベースに出力や選択を説明すること**
- **説明性の手法を考える．別のもの**
- **特徴の方は他の説明手法を**
- **今は数値を使って説明**

**読み** [推論]：4つは相互に補い合って一つのことを指している．
- 貢献の本体は「探索木ベースで出力・選択を説明する」こと
- 特徴重要度（saliency）系は別手法が担う領域であり，我々は取らない
- **説明手法そのものの設計が独立した作業として要る**（トレース抽出とは別）
- 現状は数値の羅列にとどまっている＝**埋めるべきギャップの指摘**

これは proposal.md の Phase II（トレース → 構造化された人間検証可能な説明への翻訳パイプライン，
反実仮想介入で検証）および Phase III（テンプレートベースの言語化）に対応する．

---

## 4. 現在のフェーズ判断 [推論]

Phase I（トレース抽出基盤：tree_json + 三面ビューア）は**完成している**．
発表の核になるのは **Phase II の説明パイプライン設計**．
既存成果（VL バグ発見，n スイープ実測，GUI）は単体では実装報告にとどまるが，
パイプライン設計を出せば「その設計を支える基盤・実証」として位置づけが立つ．

---

## 4b. 記述の約束事 [事実：2026-07-21 決定]

- **コミットメッセージは日本語**．句読点は「．」「，」を使う．
  2026-07-20 の Task 1 に書いた「小文字 imperative（MiniZero upstream 準拠）」は**撤回**．
- **英語で書くのは upstream に出す issue の本文だけ**．それ以外（コミット，docs，コメント）は日本語．
  ただしソースコード中のコメントは upstream に出す可能性があるため英語のままとする．

## 5. 未確認事項

1. **学会の締切と形式**（予稿のみか口頭発表ありか，ページ数）．
   これによって忠実性検証をどこまで入れるかが変わる．[要確認]
2. ~~上位2手の訪問数タイ（§2.3）~~ → 実測で確定済み．
3. ~~コーパス生成の設計判断~~ → **決定済み**（§2.2.1）．3手固定・4系統・n=16/32/128．
   対局数を増やす場合は 14 系統からの追加選定になる．